{
  "name": "muksmusic-server",
  "version": "1.0.0",
  "private": true,
  "description": "Local scraping backend for the muksmusic iOS app. Searches YouTube and streams audio via a system-installed yt-dlp so the phone can download and store songs locally.",
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  }
}
